#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int calc(vector<int> &){

}

int main ()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	
	freopen("input.txt","r",stdin);
	
	int numOperands;
	cin >> numOperands;
	vector<int> numbers;
	for(int i=0;i<numOperands;i++) cin>>numbers[i];
	
	vector<int> op;
	for()
	
	return 0;
}
